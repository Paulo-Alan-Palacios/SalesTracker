{
  "name": "salestracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run setup && npm run dev",
    "setup": "node scripts/setup.js",
    "dev": "concurrently --names backend,frontend --prefix-colors blue,green \"npm run dev --prefix backend\" \"wait-on http://localhost:3000/health && npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^8.0.0"
  }
}
